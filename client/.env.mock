# Loaded by `npm run dev:mock` and `npm run build:mock`.
# Lets the frontend run with no backend attached.
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=https://trossassignapi.vercel.app/api
VITE_MOCK_LATENCY=550
