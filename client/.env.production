# Production environment — used by Vite when building for Vercel.
# /api routes to the serverless function on the same Vercel deployment.
VITE_API_BASE_URL=https://trossassignapi.vercel.app/api
VITE_USE_MOCK_API=false
